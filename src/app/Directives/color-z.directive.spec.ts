import { ColorZDirective } from './color-z.directive';

describe('ColorZDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorZDirective();
    expect(directive).toBeTruthy();
  });
});
